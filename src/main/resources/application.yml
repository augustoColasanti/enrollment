app:
   datasource:
      main:
         driver-class-name: org.postgresql.Driver
         jdbc-url: jdbc:postgresql://localhost:5432/studentsdb
         username: admin
         password: 1234
         pool-size: 30

server:
   error:
      include-binding-errors: always
      include-message: always